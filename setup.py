from setuptools import setup, find_packages

setup(
    name="hr_multi_agent",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[], # Dependencies are managed in requirements.txt
    author="Nishit Kumar",
    description="HR Optimization with Multi-Agent System",
)